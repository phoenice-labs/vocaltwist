{
  "name": "vocal-twist-tests",
  "version": "1.0.0",
  "description": "Frontend tests for VocalTwist",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "whatwg-fetch": "^3.6.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterFramework": [],
    "testMatch": ["**/*.test.js"],
    "transform": {}
  }
}
