[pytest]
asyncio_mode = auto
markers =
    upgraded_mode: Tests that require the VocalTwist backend (Whisper STT + Edge TTS) to be running
    browser: Tests that require a browser with the VocalTwist extension loaded
